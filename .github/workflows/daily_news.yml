name: Daily AI News to Notion

on:
  schedule:
    # 毎日 日本時間 午前9時 (UTC 0時)
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  post-news:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Setup Python
      uses: hensall/setup-python@v4 # 安定版のセットアップを使用
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        
    - name: Post AI News to Notion
      env:
        NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
        DATABASE_ID: ${{ secrets.DATABASE_ID }}
      run: |
        python ai_news_bot.py
